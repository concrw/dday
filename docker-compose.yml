services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: dday
      POSTGRES_USER: dday
      POSTGRES_PASSWORD: dday
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
